# tests/

Pytest suite for `template_search_project` `src/`. Targets 90% coverage on `src` (see project `pyproject.toml`).

```bash
uv run pytest projects/templates/template_search_project/tests/ -v
```

Details: [AGENTS.md](AGENTS.md).
